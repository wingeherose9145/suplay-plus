plugins {
    id 'com.android.application' version '8.5.0' apply false   // 进一步降到更稳定版本
    id 'org.jetbrains.kotlin.android' version '2.0.0' apply false
}

tasks.register('clean', Delete) {
    delete rootProject.buildDir
}
