// Top-level build file
plugins {
    id 'com.android.application' version '8.6.0' apply false  // 降到稳定版 8.6.0
    id 'org.jetbrains.kotlin.android' version '2.0.0' apply false
}

tasks.register('clean', Delete) {
    delete rootProject.buildDir
}
